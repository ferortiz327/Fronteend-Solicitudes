
export enum IconAlerts {
  success = "success",
  info = "info",
  warning = "warning",
  error = "error",
  question = "question"
}

export enum HttpErrorCodes {
  OK = 200,
  NOT_FOUND = 404,
  ERROR = 500
}

export enum StorageKeys {
  X_BONITA_API_TOKEN = "X-Bonita-API-Token",
  USER_DETAIL = "USER_DETAIL"
}
